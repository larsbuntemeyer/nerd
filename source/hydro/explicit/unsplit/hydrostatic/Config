#	Config file for explicit hydrodynamics.  Available sub-modules are:

#	  split		Directionally split hydro schemes
#	  unsplit	Non-split schemes, which operate on all coordinate
#			directions simultaneously
#         delta_form    generally, hydro schemes which return du



REQUIRES grid/spherical/hybrid-sigma
REQUIRES driver/leapfrog

